name: flutter_tetris
description: Tetris powered by Flutter

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.1
  flutter_lints: ^1.0.4
  flutter_riverpod: ^2.0.0-dev.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

